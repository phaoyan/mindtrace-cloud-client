import React from 'react';
import {ExamResult} from "../../../../../service/data/mastery/ExamResult";

const GptAnalysisCard = (props:{examResult: ExamResult}) => {
    return (
        <div>

        </div>
    );
};

export default GptAnalysisCard;